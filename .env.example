# FormSubmit works directly from the frontend and does not require a backend or API key.
# The form posts directly to the free service endpoint.
# No environment variables are required for this setup.
